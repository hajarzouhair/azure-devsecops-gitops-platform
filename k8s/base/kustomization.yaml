apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - secret-provider-class.yaml
  - serviceaccount.yaml

labels:
  - pairs:
      managed-by: kustomize
      project: portfolio
