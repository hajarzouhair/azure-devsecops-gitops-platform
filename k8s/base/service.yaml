apiVersion: v1
kind: Service
metadata:
  name: portfolio-app
  labels:
    app: portfolio-app
spec:
  type: ClusterIP
  selector:
    app: portfolio-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
