apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: portfolio-app-secrets
  namespace: dev
spec:
  provider: azure
  parameters:
    clientID: "${SERVICE_ACCOUNT_CLIENT_ID}"   # résolu dynamiquement, plus jamais à mettre à jour
    keyvaultName: "kv-hajarazureproje-dev"
    tenantId: "7e337a63-5c53-4eaa-b9ad-54ecb1cc48f2"
    objects: |
      array:
        - |
          objectName: demo-secret
          objectType: secret
