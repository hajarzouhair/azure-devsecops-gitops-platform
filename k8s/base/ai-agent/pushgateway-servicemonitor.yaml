apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pushgateway
  namespace: ai-agent
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels: { app: pushgateway }
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
